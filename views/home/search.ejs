<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" href="https://i.ibb.co/5kX6vJr/tehei-icon.jpg" alt="tehei-icon">
    <link rel='stylesheet' href='../css/home.css' />
    <script src="https://code.jquery.com/jquery-3.1.1.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script> 
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.quicksearch/2.3.1/jquery.quicksearch.js"></script>
    <title> Home</title>

</head>

<body>
    <main id="bodyMain">
        <header>
            <!-- logo-->
            <!--    <li> <a href="/curriculo"> Meu curriculo</a> -->
            <% include ../header.ejs %>
        </header>

        <div class="container">
            
            <header class="topo">
                <h2>Filme</h2>
            </header>
            <% if(results){  if(list.length > 0){ %>           
                    <b>Resultados da pesquisa por <%= search %>:</b>              
                <div class="items">
                    <% list.forEach(function(filme) { %>
                        <article class="movie"> 
                            <div class="poster">
                                <a href="/sinopse/index/<%- filme._id %>?_method=PUT"> <img src=<%= filme.capa %> alt=<%= filme.nome %> > </a> 
                            </div>
                            <div class="data"> 
                                <a href="/sinopse/index/<%= filme.id %>" class="link_data"><h3><%= filme.nome %></h3></a> 
                            </div>
                        </article>
                    <% }); %>
                    <% }else{ %>
                        <b>Nenhum resultado encontrado para <%= search %>!</b>
                    <% }} %>
                </div>
            

        </div>
        <script>
             $('input#txtFld_pesquisar').quicksearch('div.items article');
             var isVisible = $( ".data" ).is( ":visible" );
                var isHidden = $( ".data" ).is( ":hidden" );
                if(isHidden){
                    var poster = document.querySelector('.poster')
                    poster.isHidden;
                }

        </script>
        <footer>
            <% include ../footer.ejs %>
        </footer>
    </main>
</body>

</html>